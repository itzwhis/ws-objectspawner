Config = {}

-- Command to open the spawner menu
Config.Command = 'object'

-- Admin permission required (rsg-core ace permission group)
Config.AdminGroup = 'admin'

-- Max distance (meters) for laser raycast placement
Config.LaserDistance = 50.0

-- Render distance for spawned saved objects
Config.RenderDistance = 150.0

-- Step size for keyboard fine adjustment in placement mode
Config.MoveStep = 0.05
Config.RotateStep = 2.5
